# Устойчивый install в Docker / PaaS (npm ci «Exit handler never called» часто от сети/реестра).
fetch-retries=5
fetch-retry-mintimeout=20000
fetch-retry-maxtimeout=120000
fetch-timeout=300000
audit=false
fund=false
update-notifier=false
