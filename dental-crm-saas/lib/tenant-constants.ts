/** id строки `Tenant` по slug `default` (создаётся в `scripts/backfill-tenant-ids.cjs`) */
export const DEFAULT_TENANT_ID = "cltenantdefault0000000000";

/** slug владельца лаба / одиночной БД */
export const DEFAULT_TENANT_SLUG = "default";
