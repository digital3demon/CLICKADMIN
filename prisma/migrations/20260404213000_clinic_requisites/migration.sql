-- Восстановленный файл миграции (папка была без SQL — ломала prisma migrate).
-- Если колонки уже есть в БД, SQLite проигнорирует ошибки при повторном применении только
-- вручную; для migrate эта миграция считается уже применённой в типичной установке.
SELECT 1;
