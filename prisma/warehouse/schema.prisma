generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "sqlite"
  url          = env("WAREHOUSE_DATABASE_URL")
  relationMode = "prisma"
}

// Warehouse domain schema scaffold.

