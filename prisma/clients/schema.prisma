generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider     = "sqlite"
  url          = env("CLIENTS_DATABASE_URL")
  relationMode = "prisma"
}

// Clients domain schema scaffold.
// Runtime currently uses generated root client and datasource override.

